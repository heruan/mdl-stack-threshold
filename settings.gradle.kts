rootProject.name = "mdl-stack-threshold"

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        maven("https://maven.vaadin.com/vaadin-prereleases")
    }
}
